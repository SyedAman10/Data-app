import styled from 'styled-components';
import {View} from 'react-native';

export const SelectionView = styled(View)`
  flex: 1;
  align-items: center;
`;

export const SelectionScreenContainer = styled(View)`
  align-items: center;
  width: 80%;
`;
