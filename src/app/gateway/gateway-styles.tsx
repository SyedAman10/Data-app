import styled from 'styled-components';
import {View} from 'react-native';

export const GatewayView = styled(View)`
  flex: 1;
  align-items: center;
`;

export const GatewayContainer = styled(View)`
  align-items: center;
  width: 80%;
`;
